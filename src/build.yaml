targets:
  $default:
    builders:
      source_gen:combining_builder:
        options:
          preamble: |
            // coverage:ignore-file

global_options:
  dart_mappable_builder:
    options:
      caseStyle: snakeCase
